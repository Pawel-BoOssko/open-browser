# OpenBridge Environment

This directory documents the runtime environment and capabilities of the OpenBridge/Open Browser system.
The system is designed with a principle of self-discovery, allowing LLM instances to query the Host for available capabilities, models, and protocols.
